export async function reviewCommand(
  projectPath: string,
  opts: { accept?: boolean; reject?: boolean },
): Promise<void> {
  throw new Error("`dream review` is not implemented yet (Milestone 5).");
}
