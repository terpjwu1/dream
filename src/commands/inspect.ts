export async function inspectCommand(
  projectPath: string,
  opts: { since?: string; session?: string },
): Promise<void> {
  throw new Error("`dream inspect` is not implemented yet (Milestone 1).");
}
